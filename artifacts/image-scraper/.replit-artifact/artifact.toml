kind = "web"
previewPath = "/"
title = "Image Scraper"
version = "1.0.0"
id = "artifacts/image-scraper"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20041

[services.development]
run = "pnpm --filter @workspace/image-scraper run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/image-scraper", "run", "build" ]
publicDir = "artifacts/image-scraper/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20041"
BASE_PATH = "/"
